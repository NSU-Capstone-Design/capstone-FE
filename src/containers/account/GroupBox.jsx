import React, { useState } from 'react';
import { makeStyles } from '@material-ui/core';
import { group } from '../../reducers/account/groupCreate';
import { makeStyles } from '@material-ui/core';

const useStyle = makeStyles();

const GroupList = (data) => {};